# CUDA Matmul 项目 —— 交接文档 v2

> ⚠️ **历史交接文档（GTX 1650S / Turing CC 7.5 时期）。** 本文是项目早期在 1650S 上开发时的交接记录，其硬件参数、目录结构、bench id 编号、GFLOPS 数字均为当时实测，**在 H20（CC 9.0）上已全部过时**。最新 H20 真实复现见 docs 00–13 与 [H20复现结论](H20%E5%A4%8D%E7%8E%B0%E7%BB%93%E8%AE%BA.md)，逐 kernel 的 H20 ncu 分析见 docs 02–07。本文仅作开发历程与方法论参考保留。

> 新开对话时把本文件贴给 Claude,即可无缝续上。
> 配合项目原则(不直接给完整 kernel 代码、ncu 驱动分析、自己提假设)使用。
> 本版相对 v1 的主要更新:Kernel 4(2D Block Tiling)完成定版,并记录了 8×8 spilling 反例。

---

## 一、硬件环境(注意:本机实测显示为 20 SM,与 v1 记录的 14 SM 不符,以 ncu 为准)

- **当前显卡:GTX 1650 (Super),WSL2/WDDM 环境**(原计划的 4090/3090 已不可用)
- 架构 Turing,compute capability **7.5** → 编译用 `-arch=sm_75`
- FP32 峰值约 4–5 TFLOPS,显存 4GB,L2 仅 ~1MB
- **ncu 实测:20 个 SM**(GPU Speed Of Light 段 `# SMs = 20`),每 SM 65536 寄存器
- CUDA 工具链可用,nvcc 正常
- **限制:compute-sanitizer 在 WDDM 下无法精确定位越界**(报 "Failed to initialize WDDM debugger interface")。ncu 可用(`--set basic` 已验证可跑,`--metrics` 单项也可跑)。
- **所有性能里程碑以本机 cuBLAS 为锚**,siboehm 的 3090/4090 标称值全部作废。比的是同机相对提升。

---

## 二、工程结构(已搭好,稳定)

```
CUDA_MATMUAL/
├── include/
│   ├── common.h      # CHECK_CUDA / CHECK_CUBLAS 宏、CEIL_DIV、KernelFn typedef、init_matrix 声明
│   └── kernels.h     # 所有 launcher 的声明
├── kernels/
│   ├── 01_naive.cu          # naive_kernel + launch_naive_kernel
│   ├── 02_smem.cu           # smem_kernel + launch_smem_kernel
│   ├── 03_blocktiling.cu    # blocktiling_kernel + launch_blocktiling_kernel (1D)
│   └── 04_2Dblocktiling.cu  # Dblocktiling_kernel + launch_2Dblocktiling_kernel (2D)
├── src/
│   ├── benchmark.cu  # 性能测试 main:cudaEvent 计时 + 注册表分发
│   ├── verify.cu     # 正确性验证 main:cuBLAS 对拍 + 相对误差判据
│   └── utils.cu      # init_matrix 定义(暂未拆分,见待办)
└── Makefile          # 分离编译,bench + verify 两个 target,verify 链 -lcublas
```

### 关键工程约定(不变)
- **B 路线**:所有 kernel 编进一个 bin,benchmark/verify 各自维护注册表,按 id 分发。
- **绝不 `#include .cu` 文件**(踩过坑:multiple definition)。跨文件只靠 kernels.h 声明。
- 调用约定:`./kernels/cuda_core/bench <id> M N K`、`./kernels/cuda_core/verify <id> M N K`,无参跑默认 1024³ id=0。
- 编译:`-arch=sm_75 -I include -O3`,改头文件需 `make clean`。

### 注册表当前内容
- benchmark.cu: id 0/1/2/3/4 = dummy / naive / smem / blocktiling(1D) / **Dblocktiling(2D)**
- verify.cu: id 0/1/2/3/4 = **cublas_ref** / naive / smem / blocktiling(1D) / **Dblocktiling(2D)**
  - **注意:两文件 id 映射不同!** verify 的 id=0 是 cublas reference。
  - **Kernel 4(2D)在两个文件里都是 id=4**(实测用的就是 `./kernels/cuda_core/bench 4` 和 `./kernels/cuda_core/verify 4`)。

### verify 判据(不变)
- 相对误差:`|mine-ref| / (|ref|+1e-5) < 1e-2`
- cuBLAS 列主序技巧:A/B 交换、用 N,M,K 顺序、leading dim 见 verify.cu
- 健康标志:小尺寸(256)出现 ~1e-3 非零小误差证明 verify 有鉴别力。Kernel 4 实测 256³ 的 max_rel_err = 3.2e-3、bad_count = 0,符合健康标志。

---

## 三、已完成 Kernel 与性能(GTX 1650S,4096³ FP32,本机 ncu)

| id | Kernel              | GFLOPS | 相对 naive | 相对前档 | verify | 对应 siboehm |
|----|---------------------|--------|-----------|---------|--------|-------------|
| 1  | Naive               | 496    | 1.0×      | —       | PASS   | Kernel 1    |
| 2  | SMEM Caching        | 639    | 1.29×     | 1.29×   | PASS   | Kernel 3    |
| 3  | 1D Block Tiling     | 1279   | 2.58×     | 2.00×   | PASS   | Kernel 4    |
| 4  | **2D Block Tiling (8×4)** | **2308** | **4.65×** | **1.80×** | PASS | Kernel 5 |

> 跳过 siboehm Kernel 2(GMEM Coalescing):ncu 证实 naive 线程映射已天然合并(DRAM 仅 21%)。

### Kernel 4(2D Block Tiling)定版配置 —— **TM=8, TN=4**
- `BM=64, BN=64, BK=8, TM=8, TN=4`
- blockDim = **(16,8) = 128 线程**;grid = CEIL_DIV(N,64)×CEIL_DIV(M,64)
- 一个 thread 算 C 里一个 **TM×TN = 8(行)×4(列)** 的小矩形
- **核心机制(2D vs 1D 的灵魂)**:内层先把 `regA[TM]`、`regB[TN]` 预取进寄存器,再做 TM×TN 次乘加,全程不碰 shared → **A 和 B 同时复用**(1D 只有 B 复用)
- 访存比:`(TM+TN):(TM*TN) = 12:32 = 1:2.67`(1D 是 ~1.1:1)

#### 三套坐标真值表(易错命门,务必查表不靠记忆)
| 量 | 表达式(blockDim=16×8) |
|---|---|
| 列方向 thread 数 | BN/TN = 16 |
| 行方向 thread 数 | BM/TM = 8 |
| 列序号 | `tid % 16` |
| 行序号 | `tid / 16` |
| 行起点(取 A / 写 C) | `(tid/16)*TM` |
| 列起点(取 B / 写 C) | `(tid%16)*TN` |
| acc 展平 | `acc[i*TM+j]`,i 管列(范围 TN)、j 管行(范围 TM) |

- **铁律**:取 A 的行 = 写 C 的行;取 B 的列 = 写 C 的列。三套坐标必须用同一套。
- **搬运**:128 线程搬 As/Bs 各 512 个元素 → 每线程搬 4 个,`idx = tid + load*128`,`load<4`,左边 shared 下标用 idx 算出的 as_r/as_c(不是 tid),右边 global 地址要带 `blockIdx.y*BM` / `bk` / `blockIdx.x*BN` 偏移 + 边界判断补 0。

#### Kernel 4 的 ncu 画像(4096³)
- L1/TEX **98.34%**(↑,从 1D 的 81%)—— **新瓶颈 = SRAM 子系统饱和**
- Compute (SM) 53%(↑,从 1D 的 48%)
- DRAM 44%(↑,从 1D 的 27%)
- Registers/Thread = **96**;Achieved Occupancy = **62%**(↑,从 1D 的 50%)
- **关键洞察**:occupancy 不降反升,因为 block 从 512→128 线程,单 block 寄存器总账更轻(128×96=12288),SM 能塞 5 个 block(Block Limit Registers=5)。**"tile 与线程排布解耦" 的深层回报。**
- 加速 1.8× 来源是双重的:访存比改善 + occupancy 意外提升。

---

## 四、重要反例:8×8 过界 spilling(博客高光,务必保留)

试过把 TN 4→8(blockDim 改 8×8=64 线程,搬运改每线程搬 8 个、`load<8` 步长 64)。

| 指标 | 8×4(赢) | 8×8(输) |
|---|---|---|
| GFLOPS | **2308** | **1358**(↓,退回 1D 水平) |
| Registers/Thread | 96 | **128(触顶台阶)** |
| Compute (SM) | 53% | **31.65%(腰斩)** |
| DRAM | 44% | 27.94% |
| Occupancy | 62% | 49% |
| verify | PASS | PASS(逻辑正确,纯性能问题) |

**结论:8×8 在 1650S 上触发 register spilling。**
- acc 升到 64 个 + regA/regB 16 + 索引开销 + O3 展开临时量 → 逻辑需求 150+,编译器压在 128,装不下的 spill 到 local memory(= DRAM)。
- 内层 64 次 `acc[...] +=` 从寄存器累加变成 local memory 往返 → Compute stall → duration 62ms→111ms。
- **核心认知**:算术强度有**寄存器预算天花板**,且天花板是**单卡特性**。siboehm 3090 能吃 8×8,1650S 不行。这正是 Kernel 6(autotuning)存在的理由——甜点是扫出来的,不是推出来的。

**待补的最后一刀实锤**(下一窗口第一件事,确认后即闭环):
```
ncu --metrics l1tex__t_bytes_pipe_lsu_mem_local_op_ld.sum,l1tex__t_bytes_pipe_lsu_mem_local_op_st.sum ./kernels/cuda_core/bench 4 4096 4096 4096
```
- 预期:8×8 的 local load/store 字节数 >> 0(8×4 那版应接近 0)。非零即 spilling 实锤。
- 注意:目前 04 文件里 TN 需确认是 4 还是 8。要测 8×8 需临时改回 8 重编;**定版应保持 TN=4**。

---

## 五、关键技术认知(已内化,本版新增标 ★)

1. ncu "Memory Throughput" 取内存子系统最忙一级,不等于 DRAM。
2. shared memory 和 L1 同一块物理 SRAM,ncu 的 L1/TEX 把 shared 访问也算进去。
3. 三级数据流 global → shared → register:shared 解决 block 级复用,register 解决 thread 级复用。
4. tile 大小(BM×BN)与线程排布(blockDim)解耦;2D tiling 让线程数大降而单线程活变多。
5. 行主序索引:`B[行][列]=行*N+列`(K×N 乘 N);`A[行][列]=行*K+列`(M×K 乘 K)。
6. 小尺寸 + 大 L2 掩盖访存瓶颈,性能锚点必须用大尺寸(256³ 仅 829 GFLOPS vs 4096³ 2308)。
7. ★ **L1/TEX throughput 受"访问指令数"驱动,不是数据字节量**。LSU 瓶颈是指令发射率。这是 float4 能卸压的根据。
8. ★ **throughput 是比率(流量÷时间),kernel 被 stall 拖慢时,所有 throughput 看着都"不忙"**。所以 spilling 不能靠 SOL 百分比判断,必须直接量 local memory 流量。
9. ★ **算术强度有寄存器预算天花板**:acc 留在寄存器是 2D tiling 收益成立的隐含前提,一旦 spill 到 local(DRAM),理论收益清零甚至倒亏。

---

## 六、待办 / 下一步

### 立即下一步:Kernel 5 —— float4 向量化访存(siboehm Kernel 6)
- **动机**:直击 Kernel 4 未松动的 **L1/TEX 98% 瓶颈**。一条 `LDS.128` 搬 4 个 float,把 shared 访问**指令数砍到 1/4**(数据量不变),给 LSU 卸压。
- **预测(待验证)**:float4 后 L1/TEX 明显下降,DRAM/Compute 不变或微升。
- **两个硬约束(这是需要"全新 API 讲解"的地方)**:
  1. **对齐**:float4 访问要求地址 16 字节对齐(As/Bs 起址 + reinterpret_cast 下标都要落在 16B 边界)。
  2. **shared 布局可能要改**:当前取 regA 沿行方向(`As[row+i][k]`,跨行 stride=BK,**不连续**),float4 取不了不连续数据 → 可能需要 **As 转置存储**(siboehm 在此做了转置)。这是 Kernel 5 核心难点,留到写时专攻。

### 后续 Kernel(自己想方案,Claude 只给方向)
- Kernel 6: Autotuning —— 扫 BM/BN/BK/TM/TN 找单卡甜点(8×8 反例已证明必要性)。siboehm K7。
- Kernel 7: Warp Tiling。siboehm K8。
- Kernel 8: Resolving Bank Conflicts。siboehm K9。Kernel 3/4 内层 As 读怀疑有 broadcast/conflict,留此处理。
- Kernel 9: Double Buffering / cp.async。siboehm K10。

### 工程债(不急)
- 04 文件 TN 当前值需确认并定版为 4。
- 搬运循环里的 `128`、`load<4` 是硬编码总线程数,autotuning 时要参数化为 `BM*BN/threads`。
- init_matrix 定义位置:verify.cu 用随机数版、benchmark.cu 用 `i%17` 周期版,需统一或明确分工。
- 两个注册表重复维护,可抽共享文件。
- 还没写 run_all.sh / profile.sh。
- benchmark.cu 的 id 边界检查在 argv 解析前(恒 0),逻辑空转,需挪到解析后。

### 待复习的悬留概念
- 专题1 遗留:单线程条件分支发散代价(divergence cost),尚未跟进。

---

## 七、教学协议提醒(给下一个 Claude)

- 不直接给完整 kernel 代码,除非全新 API(如后续 float4、cp.async、WMMA)。
- 每个 kernel:用户先自己想方案、写代码、跑性能、做 ncu 分析、提优化假设;Claude 只给方向提示和引导性问题。
- ncu 分析让用户自己解读,错了先问引导问题不直接纠正。
- 性能里程碑:达不到合理提升不进下一步,回炉分析。**负结果(如 8×8 spilling)同样要完整记录,portfolio 价值高。**
- 每个 kernel 完成后写小结(性能/ncu metric/推导/踩坑/对比 siboehm),最终汇编成博客。
- 用户系统背景强(QEMU GPGPU 建模、RISC-V、写过 GEMM/Attention),用 syscall/内核/硬件类比讲解最有效。
- 回复控制在 400–600 字,一次 focus 一个问题,节奏跟随用户。
- **查真值表纪律**:三套坐标(取 A / 取 B / 写 C)的拆 tid 常数随 blockDim 变,务必列表查证,不靠记忆——这是用户最易栽的点(col_in_tile 用错 tid/16、写回常数残留 64 等都栽过)。