# CUDA Matmul 项目 —— 交接文档

> ⚠️ **历史交接文档（GTX 1650S / Turing CC 7.5 时期）。** 本文是项目早期在 1650S 上开发时的交接记录，其硬件参数、目录结构、bench id 编号、GFLOPS 数字均为当时实测，**在 H20（CC 9.0）上已全部过时**。最新 H20 真实复现见 docs 00–13 与 [H20复现结论](H20%E5%A4%8D%E7%8E%B0%E7%BB%93%E8%AE%BA.md)，逐 kernel 的 H20 ncu 分析见 docs 02–07。本文仅作开发历程与方法论参考保留。

> 新开对话时把本文件贴给 Claude,即可无缝续上。
> 配合项目原则(不直接给完整 kernel 代码、ncu 驱动分析、自己提假设)使用。

---

## 一、硬件环境(已变更,务必注意)

- **当前显卡:GTX 1650 Super,WSL2/WDDM 环境**(原计划的 4090 已不可用)
- 架构 Turing,compute capability **7.5** → 编译用 `-arch=sm_75`
- FP32 峰值约 4–5 TFLOPS,显存 4GB,L2 仅 ~1MB
- CUDA 工具链可用,nvcc 正常
- **限制:compute-sanitizer 在 WDDM 下无法精确定位越界**(报 "Failed to initialize WDDM debugger interface")。ncu 可用(`--set basic` 已验证可跑)。
- **所有性能里程碑以本机 cuBLAS 为锚**,siboehm 的 3090 标称值和 4090 推算全部作废。比的是同机相对提升。

---

## 二、工程结构(已搭好,稳定)

```
CUDA_MATMUAL/
├── include/
│   ├── common.h      # CHECK_CUDA / CHECK_CUBLAS 宏、CEIL_DIV、KernelFn typedef、init_matrix 声明
│   └── kernels.h     # 所有 launcher 的声明
├── kernels/
│   ├── 01_naive.cu       # naive_kernel + launch_naive_kernel
│   ├── 02_smem.cu        # smem_kernel + launch_smem_kernel
│   └── 03_blocktiling.cu # blocktiling_kernel + launch_blocktiling_kernel
├── src/
│   ├── benchmark.cu  # 性能测试 main:cudaEvent 计时 + 注册表分发
│   ├── verify.cu     # 正确性验证 main:cuBLAS 对拍 + 相对误差判据
│   └── utils.cu      # init_matrix 定义(暂未拆分,见待办)
└── Makefile          # 分离编译,bench + verify 两个 target,verify 链 -lcublas

```

### 关键工程约定
- **B 路线**:所有 kernel 编进一个 bin,benchmark/verify 各自维护注册表 `g_kernels[]` / `g_names[]`,按 id 分发。
- **绝不 `#include .cu 文件**(踩过坑:导致 multiple definition)。跨文件只靠 kernels.h 的声明。
- 每个函数定义只能存在一处;头文件只放声明、宏、typedef。
- 调用约定:`./kernels/cuda_core/bench <id> M N K`、`./kernels/cuda_core/verify <id> M N K`,无参跑默认 1024³ id=0。
- 编译:`-arch=sm_75 -I include -O3`,改头文件需 `make clean`(Makefile 已加 common.h 依赖)。

### 注册表当前内容
- benchmark.cu: `{launch_dummy_kernel, launch_naive_kernel, launch_smem_kernel, launch_blocktiling_kernel}` → id 0/1/2/3
- verify.cu: `{launch_cublas_ref, launch_naive_kernel, launch_smem_kernel, launch_blocktiling_kernel}` → id 0/1/2/3
  - **注意:两个文件的 id 映射不同!** verify 的 id=0 是 cublas reference。用 verify 时 id=1 才是 naive。

### verify 判据
- 相对误差为主:`|mine-ref| / (|ref|+1e-5) < 1e-2`
- cuBLAS 调用用列主序技巧:传参时 A/B 交换、用 `N,M,K` 顺序、leading dim 见 verify.cu
- 健康标志:小尺寸(256)应出现非零小误差(~1e-3),证明 verify 有鉴别力;大尺寸偶现 0 属正常

---

## 三、已完成 Kernel 与性能(GTX 1650S,4096³ FP32)

| id | Kernel              | GFLOPS | 相对 naive | verify | 对应 siboehm |
|----|---------------------|--------|-----------|--------|-------------|
| 1  | Naive               | 496    | 1.0×      | PASS   | Kernel 1    |
| 2  | SMEM Caching        | 639    | 1.29×     | PASS   | Kernel 3    |
| 3  | 1D Block Tiling     | 1279   | 2.58×     | PASS   | Kernel 4    |

> **跳过了 siboehm Kernel 2(GMEM Coalescing)**:ncu 证实本项目 naive 的线程映射已天然合并
> (相邻 thread 读 B 连续、读 A 广播,DRAM 仅 21%),coalescing 不是瓶颈,故直接上 SMEM。
> 这是一个有依据的决策点,portfolio 里如实记录。

### Kernel 3(1D Block Tiling)配置
- `BM=64, BN=64, BK=8, TM=8`;block = (64,8) = 512 线程;grid = CEIL_DIV(N,64)×CEIL_DIV(M,64)
- 核心:一个 thread 算同列连续 TM=8 个 C 元素,B 值读一次复用 8 次
- **三层独立索引映射**(易错点,已掌握):
  - 搬 As[BM][BK]:行 `tid/BK`、列 `tid%BK`
  - 搬 Bs[BK][BN]:行 `tid/BN`、列 `tid%BN`
  - 算 C:列 `tid%BN`、行段 `tid/BN`,行偏移 `(tid/BN)*TM + i`
  - 教训:三套坐标用不同变量名,别复用 ty/tx

---

## 四、关键技术认知(已内化)

1. **ncu 的 "Memory Throughput" 取内存子系统最忙的一级**,不等于 DRAM。naive 显示 82% 其实是 L1/TEX 忙,DRAM 只 21% → 瓶颈不是显存带宽。
2. **shared memory 和 L1 在同一块物理 SRAM**:ncu 的 L1/TEX throughput 把 shared 访问也算进去。所以 smem 版 L1/TEX 不降反升(84%→87%),因为 shared 访问占满了这块 SRAM。
3. **SMEM 版只快 29% 的原因**:数据搬进 shared 但每 thread 只算 1 元素,shared 访问比仍是 ~2:1,SRAM 仍是瓶颈。需要寄存器复用(tiling)才能真正卸载。
4. **三级数据流 global → shared → register**:shared 解决 block 级复用,register 解决 thread 级复用,两层叠加。
5. **tile 大小(BM×BN)与线程排布(blockDim)解耦**:一个 thread 算多元素后,`blockIdx.x*BN` 取代 `blockDim.x*blockIdx.x` 当列起点。
6. **测试尺寸影响结论**:小尺寸 + 大 L2 会掩盖访存瓶颈(4090 上 naive 数字诡异稳定即此故)。要用大尺寸暴露真瓶颈。
7. **行主序索引命门**:`B[行][列] = 行*N+列`(B 是 K×N,乘 N);`A[行][列]=行*K+列`(A 是 M×K,乘 K)。两个矩阵每行宽度不同。

---

## 五、待办 / 下一步

### 立即下一步:对 Kernel 3 做 ncu 分析(还没做)
- 跑 `ncu --set basic ./kernels/cuda_core/bench 3 4096 4096 4096`
- **自己解读**:1D tiling 后 L1/TEX throughput 降了没?compute 占比升了没?新瓶颈是什么?
- 预期:shared 访问压力下降,但可能仍未饱和 → 引出 Kernel 4(2D tiling)

### 接下来的 Kernel(自己想方案,Claude 只给方向)
- **Kernel 4: 2D Block Tiling** —— 一个 thread 算 TM×TN 的小块,A 和 B 都复用。
  访存比 `(TM+TN):(TM*TN)`,TM=TN=8 时 16:64=1:4,计算终于压过访存。siboehm Kernel 5。
- **Kernel 5: Vectorized Memory Access** —— float4 向量化访存。siboehm Kernel 6。
- **Kernel 6: Autotuning** —— 扫 BM/BN/BK/TM/TN。siboehm Kernel 7。
- **Kernel 7: Warp Tiling** —— siboehm Kernel 8。
- **Kernel 8: Resolving Bank Conflicts** —— siboehm Kernel 9。Kernel 3 内层 `As` 读已怀疑有 broadcast/conflict,留到此处专门处理。
- **Kernel 9: Double Buffering / cp.async** —— siboehm Kernel 10。

### 工程债(不急,有空再清)
- init_matrix 定义位置:确认在 utils.cu 还是 benchmark.cu/verify.cu,避免重复定义。verify.cu 用了随机数版,benchmark.cu 用 `i%17` 周期版——两版需统一或明确分工。
- benchmark.cu 和 verify.cu 的注册表重复维护,以后可抽到共享文件。
- 还没写 `run_all.sh` 和 `profile.sh`(原计划组件)。
- benchmark.cu 的 id 边界检查在 argv 解析前(id 恒 0),逻辑空转,需把检查挪到解析后。

### 待复习的悬留概念
- 专题1 遗留:单线程条件分支发散代价(divergence cost),尚未跟进。

---

## 六、教学协议提醒(给下一个 Claude)

- 不直接给完整 kernel 代码,除非全新 API(如后续 float4、cp.async、WMMA)
- 每个 kernel:用户先自己想方案、写代码、跑性能、做 ncu 分析、提优化假设;Claude 只给方向提示和引导性问题
- ncu 分析让用户自己解读,错了先问引导问题不直接纠正
- 性能里程碑:达不到合理提升不进下一步,回炉分析
- 每个 kernel 完成后写小结(性能/ncu metric/推导/踩坑/对比 siboehm),最终汇编成博客
- 用户系统背景强(QEMU GPGPU 建模、RISC-V、写过 GEMM/Attention),用 syscall/内核/硬件类比讲解最有效
- 回复控制在 400–600 字,一次 focus 一个问题,节奏跟随用户