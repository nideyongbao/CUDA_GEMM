/*
 * TinyFA
 * @author 	: keith@robot9.me
 *
 */

// This file is auto-generated by generate_kernels.py — do not edit manually.

#include "../launcher.cuh"

namespace tfa::impl {

// clang-format off

#ifdef TFA_TARGET_HEADDIM_32
template void runFwd<SM75, float, 32, false>(
    const float* Q, const float* K, const float* V, float* O,
    int batchSize, int seqLenQ, int seqLenKV, int numHeadsQ, int numHeadsKV,
    cudaStream_t stream);

template void runFwd<SM75, float, 32, true>(
    const float* Q, const float* K, const float* V, float* O,
    int batchSize, int seqLenQ, int seqLenKV, int numHeadsQ, int numHeadsKV,
    cudaStream_t stream);

template void runFwdVarLen<SM75, float, 32, false>(
    const float* Q, const float* K, const float* V, float* O,
    const int* cuSeqLensQ, const int* cuSeqLensKV,
    int batchSize, int maxSeqLenQ, int maxSeqLenKV,
    int numHeadsQ, int numHeadsKV,
    cudaStream_t stream);

template void runFwdVarLen<SM75, float, 32, true>(
    const float* Q, const float* K, const float* V, float* O,
    const int* cuSeqLensQ, const int* cuSeqLensKV,
    int batchSize, int maxSeqLenQ, int maxSeqLenKV,
    int numHeadsQ, int numHeadsKV,
    cudaStream_t stream);

template void runFwdPagedVarLen<SM75, float, 32, false>(
    const float* Q, float* O,
    const float* kCachePool, const float* vCachePool,
    const int* cuSeqLensQ, const int* cuSeqLensKV,
    const int* blockTable,
    int batchSize, int maxSeqLenQ, int maxSeqLenKV,
    int numHeadsQ, int numHeadsKV,
    int pageSize, int maxBlocksPerSeq,
    float* tmpO, float* tmpLse, int partitionSize, int totalQ,
    cudaStream_t stream);

template void runFwdPagedVarLen<SM75, float, 32, true>(
    const float* Q, float* O,
    const float* kCachePool, const float* vCachePool,
    const int* cuSeqLensQ, const int* cuSeqLensKV,
    const int* blockTable,
    int batchSize, int maxSeqLenQ, int maxSeqLenKV,
    int numHeadsQ, int numHeadsKV,
    int pageSize, int maxBlocksPerSeq,
    float* tmpO, float* tmpLse, int partitionSize, int totalQ,
    cudaStream_t stream);
#endif

#ifdef TFA_TARGET_HEADDIM_64
template void runFwd<SM75, float, 64, false>(
    const float* Q, const float* K, const float* V, float* O,
    int batchSize, int seqLenQ, int seqLenKV, int numHeadsQ, int numHeadsKV,
    cudaStream_t stream);

template void runFwd<SM75, float, 64, true>(
    const float* Q, const float* K, const float* V, float* O,
    int batchSize, int seqLenQ, int seqLenKV, int numHeadsQ, int numHeadsKV,
    cudaStream_t stream);

template void runFwdVarLen<SM75, float, 64, false>(
    const float* Q, const float* K, const float* V, float* O,
    const int* cuSeqLensQ, const int* cuSeqLensKV,
    int batchSize, int maxSeqLenQ, int maxSeqLenKV,
    int numHeadsQ, int numHeadsKV,
    cudaStream_t stream);

template void runFwdVarLen<SM75, float, 64, true>(
    const float* Q, const float* K, const float* V, float* O,
    const int* cuSeqLensQ, const int* cuSeqLensKV,
    int batchSize, int maxSeqLenQ, int maxSeqLenKV,
    int numHeadsQ, int numHeadsKV,
    cudaStream_t stream);

template void runFwdPagedVarLen<SM75, float, 64, false>(
    const float* Q, float* O,
    const float* kCachePool, const float* vCachePool,
    const int* cuSeqLensQ, const int* cuSeqLensKV,
    const int* blockTable,
    int batchSize, int maxSeqLenQ, int maxSeqLenKV,
    int numHeadsQ, int numHeadsKV,
    int pageSize, int maxBlocksPerSeq,
    float* tmpO, float* tmpLse, int partitionSize, int totalQ,
    cudaStream_t stream);

template void runFwdPagedVarLen<SM75, float, 64, true>(
    const float* Q, float* O,
    const float* kCachePool, const float* vCachePool,
    const int* cuSeqLensQ, const int* cuSeqLensKV,
    const int* blockTable,
    int batchSize, int maxSeqLenQ, int maxSeqLenKV,
    int numHeadsQ, int numHeadsKV,
    int pageSize, int maxBlocksPerSeq,
    float* tmpO, float* tmpLse, int partitionSize, int totalQ,
    cudaStream_t stream);
#endif

#ifdef TFA_TARGET_HEADDIM_96
template void runFwd<SM75, float, 96, false>(
    const float* Q, const float* K, const float* V, float* O,
    int batchSize, int seqLenQ, int seqLenKV, int numHeadsQ, int numHeadsKV,
    cudaStream_t stream);

template void runFwd<SM75, float, 96, true>(
    const float* Q, const float* K, const float* V, float* O,
    int batchSize, int seqLenQ, int seqLenKV, int numHeadsQ, int numHeadsKV,
    cudaStream_t stream);

template void runFwdVarLen<SM75, float, 96, false>(
    const float* Q, const float* K, const float* V, float* O,
    const int* cuSeqLensQ, const int* cuSeqLensKV,
    int batchSize, int maxSeqLenQ, int maxSeqLenKV,
    int numHeadsQ, int numHeadsKV,
    cudaStream_t stream);

template void runFwdVarLen<SM75, float, 96, true>(
    const float* Q, const float* K, const float* V, float* O,
    const int* cuSeqLensQ, const int* cuSeqLensKV,
    int batchSize, int maxSeqLenQ, int maxSeqLenKV,
    int numHeadsQ, int numHeadsKV,
    cudaStream_t stream);

template void runFwdPagedVarLen<SM75, float, 96, false>(
    const float* Q, float* O,
    const float* kCachePool, const float* vCachePool,
    const int* cuSeqLensQ, const int* cuSeqLensKV,
    const int* blockTable,
    int batchSize, int maxSeqLenQ, int maxSeqLenKV,
    int numHeadsQ, int numHeadsKV,
    int pageSize, int maxBlocksPerSeq,
    float* tmpO, float* tmpLse, int partitionSize, int totalQ,
    cudaStream_t stream);

template void runFwdPagedVarLen<SM75, float, 96, true>(
    const float* Q, float* O,
    const float* kCachePool, const float* vCachePool,
    const int* cuSeqLensQ, const int* cuSeqLensKV,
    const int* blockTable,
    int batchSize, int maxSeqLenQ, int maxSeqLenKV,
    int numHeadsQ, int numHeadsKV,
    int pageSize, int maxBlocksPerSeq,
    float* tmpO, float* tmpLse, int partitionSize, int totalQ,
    cudaStream_t stream);
#endif

#ifdef TFA_TARGET_HEADDIM_128
template void runFwd<SM75, float, 128, false>(
    const float* Q, const float* K, const float* V, float* O,
    int batchSize, int seqLenQ, int seqLenKV, int numHeadsQ, int numHeadsKV,
    cudaStream_t stream);

template void runFwd<SM75, float, 128, true>(
    const float* Q, const float* K, const float* V, float* O,
    int batchSize, int seqLenQ, int seqLenKV, int numHeadsQ, int numHeadsKV,
    cudaStream_t stream);

template void runFwdVarLen<SM75, float, 128, false>(
    const float* Q, const float* K, const float* V, float* O,
    const int* cuSeqLensQ, const int* cuSeqLensKV,
    int batchSize, int maxSeqLenQ, int maxSeqLenKV,
    int numHeadsQ, int numHeadsKV,
    cudaStream_t stream);

template void runFwdVarLen<SM75, float, 128, true>(
    const float* Q, const float* K, const float* V, float* O,
    const int* cuSeqLensQ, const int* cuSeqLensKV,
    int batchSize, int maxSeqLenQ, int maxSeqLenKV,
    int numHeadsQ, int numHeadsKV,
    cudaStream_t stream);

template void runFwdPagedVarLen<SM75, float, 128, false>(
    const float* Q, float* O,
    const float* kCachePool, const float* vCachePool,
    const int* cuSeqLensQ, const int* cuSeqLensKV,
    const int* blockTable,
    int batchSize, int maxSeqLenQ, int maxSeqLenKV,
    int numHeadsQ, int numHeadsKV,
    int pageSize, int maxBlocksPerSeq,
    float* tmpO, float* tmpLse, int partitionSize, int totalQ,
    cudaStream_t stream);

template void runFwdPagedVarLen<SM75, float, 128, true>(
    const float* Q, float* O,
    const float* kCachePool, const float* vCachePool,
    const int* cuSeqLensQ, const int* cuSeqLensKV,
    const int* blockTable,
    int batchSize, int maxSeqLenQ, int maxSeqLenKV,
    int numHeadsQ, int numHeadsKV,
    int pageSize, int maxBlocksPerSeq,
    float* tmpO, float* tmpLse, int partitionSize, int totalQ,
    cudaStream_t stream);
#endif

#ifdef TFA_TARGET_HEADDIM_192
template void runFwd<SM75, float, 192, false>(
    const float* Q, const float* K, const float* V, float* O,
    int batchSize, int seqLenQ, int seqLenKV, int numHeadsQ, int numHeadsKV,
    cudaStream_t stream);

template void runFwd<SM75, float, 192, true>(
    const float* Q, const float* K, const float* V, float* O,
    int batchSize, int seqLenQ, int seqLenKV, int numHeadsQ, int numHeadsKV,
    cudaStream_t stream);

template void runFwdVarLen<SM75, float, 192, false>(
    const float* Q, const float* K, const float* V, float* O,
    const int* cuSeqLensQ, const int* cuSeqLensKV,
    int batchSize, int maxSeqLenQ, int maxSeqLenKV,
    int numHeadsQ, int numHeadsKV,
    cudaStream_t stream);

template void runFwdVarLen<SM75, float, 192, true>(
    const float* Q, const float* K, const float* V, float* O,
    const int* cuSeqLensQ, const int* cuSeqLensKV,
    int batchSize, int maxSeqLenQ, int maxSeqLenKV,
    int numHeadsQ, int numHeadsKV,
    cudaStream_t stream);

template void runFwdPagedVarLen<SM75, float, 192, false>(
    const float* Q, float* O,
    const float* kCachePool, const float* vCachePool,
    const int* cuSeqLensQ, const int* cuSeqLensKV,
    const int* blockTable,
    int batchSize, int maxSeqLenQ, int maxSeqLenKV,
    int numHeadsQ, int numHeadsKV,
    int pageSize, int maxBlocksPerSeq,
    float* tmpO, float* tmpLse, int partitionSize, int totalQ,
    cudaStream_t stream);

template void runFwdPagedVarLen<SM75, float, 192, true>(
    const float* Q, float* O,
    const float* kCachePool, const float* vCachePool,
    const int* cuSeqLensQ, const int* cuSeqLensKV,
    const int* blockTable,
    int batchSize, int maxSeqLenQ, int maxSeqLenKV,
    int numHeadsQ, int numHeadsKV,
    int pageSize, int maxBlocksPerSeq,
    float* tmpO, float* tmpLse, int partitionSize, int totalQ,
    cudaStream_t stream);
#endif

#ifdef TFA_TARGET_HEADDIM_256
template void runFwd<SM75, float, 256, false>(
    const float* Q, const float* K, const float* V, float* O,
    int batchSize, int seqLenQ, int seqLenKV, int numHeadsQ, int numHeadsKV,
    cudaStream_t stream);

template void runFwd<SM75, float, 256, true>(
    const float* Q, const float* K, const float* V, float* O,
    int batchSize, int seqLenQ, int seqLenKV, int numHeadsQ, int numHeadsKV,
    cudaStream_t stream);

template void runFwdVarLen<SM75, float, 256, false>(
    const float* Q, const float* K, const float* V, float* O,
    const int* cuSeqLensQ, const int* cuSeqLensKV,
    int batchSize, int maxSeqLenQ, int maxSeqLenKV,
    int numHeadsQ, int numHeadsKV,
    cudaStream_t stream);

template void runFwdVarLen<SM75, float, 256, true>(
    const float* Q, const float* K, const float* V, float* O,
    const int* cuSeqLensQ, const int* cuSeqLensKV,
    int batchSize, int maxSeqLenQ, int maxSeqLenKV,
    int numHeadsQ, int numHeadsKV,
    cudaStream_t stream);

template void runFwdPagedVarLen<SM75, float, 256, false>(
    const float* Q, float* O,
    const float* kCachePool, const float* vCachePool,
    const int* cuSeqLensQ, const int* cuSeqLensKV,
    const int* blockTable,
    int batchSize, int maxSeqLenQ, int maxSeqLenKV,
    int numHeadsQ, int numHeadsKV,
    int pageSize, int maxBlocksPerSeq,
    float* tmpO, float* tmpLse, int partitionSize, int totalQ,
    cudaStream_t stream);

template void runFwdPagedVarLen<SM75, float, 256, true>(
    const float* Q, float* O,
    const float* kCachePool, const float* vCachePool,
    const int* cuSeqLensQ, const int* cuSeqLensKV,
    const int* blockTable,
    int batchSize, int maxSeqLenQ, int maxSeqLenKV,
    int numHeadsQ, int numHeadsKV,
    int pageSize, int maxBlocksPerSeq,
    float* tmpO, float* tmpLse, int partitionSize, int totalQ,
    cudaStream_t stream);
#endif

// clang-format on

}  // namespace tfa::impl
