/*
 * TinyFA
 * @author 	: keith@robot9.me
 *
 */

// This file is auto-generated by generate_kernels.py — do not edit manually.

#include "../launcher.cuh"

namespace tfa::impl {

// clang-format off

#ifdef TFA_TARGET_HEADDIM_32
template void runFwd<SM8x, __half, 32, false>(
    const __half* Q, const __half* K, const __half* V, __half* O,
    int batchSize, int seqLenQ, int seqLenKV, int numHeadsQ, int numHeadsKV,
    cudaStream_t stream);

template void runFwd<SM8x, __half, 32, true>(
    const __half* Q, const __half* K, const __half* V, __half* O,
    int batchSize, int seqLenQ, int seqLenKV, int numHeadsQ, int numHeadsKV,
    cudaStream_t stream);

template void runFwdVarLen<SM8x, __half, 32, false>(
    const __half* Q, const __half* K, const __half* V, __half* O,
    const int* cuSeqLensQ, const int* cuSeqLensKV,
    int batchSize, int maxSeqLenQ, int maxSeqLenKV,
    int numHeadsQ, int numHeadsKV,
    cudaStream_t stream);

template void runFwdVarLen<SM8x, __half, 32, true>(
    const __half* Q, const __half* K, const __half* V, __half* O,
    const int* cuSeqLensQ, const int* cuSeqLensKV,
    int batchSize, int maxSeqLenQ, int maxSeqLenKV,
    int numHeadsQ, int numHeadsKV,
    cudaStream_t stream);

template void runFwdPagedVarLen<SM8x, __half, 32, false>(
    const __half* Q, __half* O,
    const __half* kCachePool, const __half* vCachePool,
    const int* cuSeqLensQ, const int* cuSeqLensKV,
    const int* blockTable,
    int batchSize, int maxSeqLenQ, int maxSeqLenKV,
    int numHeadsQ, int numHeadsKV,
    int pageSize, int maxBlocksPerSeq,
    float* tmpO, float* tmpLse, int partitionSize, int totalQ,
    cudaStream_t stream);

template void runFwdPagedVarLen<SM8x, __half, 32, true>(
    const __half* Q, __half* O,
    const __half* kCachePool, const __half* vCachePool,
    const int* cuSeqLensQ, const int* cuSeqLensKV,
    const int* blockTable,
    int batchSize, int maxSeqLenQ, int maxSeqLenKV,
    int numHeadsQ, int numHeadsKV,
    int pageSize, int maxBlocksPerSeq,
    float* tmpO, float* tmpLse, int partitionSize, int totalQ,
    cudaStream_t stream);
#endif

#ifdef TFA_TARGET_HEADDIM_64
template void runFwd<SM8x, __half, 64, false>(
    const __half* Q, const __half* K, const __half* V, __half* O,
    int batchSize, int seqLenQ, int seqLenKV, int numHeadsQ, int numHeadsKV,
    cudaStream_t stream);

template void runFwd<SM8x, __half, 64, true>(
    const __half* Q, const __half* K, const __half* V, __half* O,
    int batchSize, int seqLenQ, int seqLenKV, int numHeadsQ, int numHeadsKV,
    cudaStream_t stream);

template void runFwdVarLen<SM8x, __half, 64, false>(
    const __half* Q, const __half* K, const __half* V, __half* O,
    const int* cuSeqLensQ, const int* cuSeqLensKV,
    int batchSize, int maxSeqLenQ, int maxSeqLenKV,
    int numHeadsQ, int numHeadsKV,
    cudaStream_t stream);

template void runFwdVarLen<SM8x, __half, 64, true>(
    const __half* Q, const __half* K, const __half* V, __half* O,
    const int* cuSeqLensQ, const int* cuSeqLensKV,
    int batchSize, int maxSeqLenQ, int maxSeqLenKV,
    int numHeadsQ, int numHeadsKV,
    cudaStream_t stream);

template void runFwdPagedVarLen<SM8x, __half, 64, false>(
    const __half* Q, __half* O,
    const __half* kCachePool, const __half* vCachePool,
    const int* cuSeqLensQ, const int* cuSeqLensKV,
    const int* blockTable,
    int batchSize, int maxSeqLenQ, int maxSeqLenKV,
    int numHeadsQ, int numHeadsKV,
    int pageSize, int maxBlocksPerSeq,
    float* tmpO, float* tmpLse, int partitionSize, int totalQ,
    cudaStream_t stream);

template void runFwdPagedVarLen<SM8x, __half, 64, true>(
    const __half* Q, __half* O,
    const __half* kCachePool, const __half* vCachePool,
    const int* cuSeqLensQ, const int* cuSeqLensKV,
    const int* blockTable,
    int batchSize, int maxSeqLenQ, int maxSeqLenKV,
    int numHeadsQ, int numHeadsKV,
    int pageSize, int maxBlocksPerSeq,
    float* tmpO, float* tmpLse, int partitionSize, int totalQ,
    cudaStream_t stream);
#endif

#ifdef TFA_TARGET_HEADDIM_96
template void runFwd<SM8x, __half, 96, false>(
    const __half* Q, const __half* K, const __half* V, __half* O,
    int batchSize, int seqLenQ, int seqLenKV, int numHeadsQ, int numHeadsKV,
    cudaStream_t stream);

template void runFwd<SM8x, __half, 96, true>(
    const __half* Q, const __half* K, const __half* V, __half* O,
    int batchSize, int seqLenQ, int seqLenKV, int numHeadsQ, int numHeadsKV,
    cudaStream_t stream);

template void runFwdVarLen<SM8x, __half, 96, false>(
    const __half* Q, const __half* K, const __half* V, __half* O,
    const int* cuSeqLensQ, const int* cuSeqLensKV,
    int batchSize, int maxSeqLenQ, int maxSeqLenKV,
    int numHeadsQ, int numHeadsKV,
    cudaStream_t stream);

template void runFwdVarLen<SM8x, __half, 96, true>(
    const __half* Q, const __half* K, const __half* V, __half* O,
    const int* cuSeqLensQ, const int* cuSeqLensKV,
    int batchSize, int maxSeqLenQ, int maxSeqLenKV,
    int numHeadsQ, int numHeadsKV,
    cudaStream_t stream);

template void runFwdPagedVarLen<SM8x, __half, 96, false>(
    const __half* Q, __half* O,
    const __half* kCachePool, const __half* vCachePool,
    const int* cuSeqLensQ, const int* cuSeqLensKV,
    const int* blockTable,
    int batchSize, int maxSeqLenQ, int maxSeqLenKV,
    int numHeadsQ, int numHeadsKV,
    int pageSize, int maxBlocksPerSeq,
    float* tmpO, float* tmpLse, int partitionSize, int totalQ,
    cudaStream_t stream);

template void runFwdPagedVarLen<SM8x, __half, 96, true>(
    const __half* Q, __half* O,
    const __half* kCachePool, const __half* vCachePool,
    const int* cuSeqLensQ, const int* cuSeqLensKV,
    const int* blockTable,
    int batchSize, int maxSeqLenQ, int maxSeqLenKV,
    int numHeadsQ, int numHeadsKV,
    int pageSize, int maxBlocksPerSeq,
    float* tmpO, float* tmpLse, int partitionSize, int totalQ,
    cudaStream_t stream);
#endif

#ifdef TFA_TARGET_HEADDIM_128
template void runFwd<SM8x, __half, 128, false>(
    const __half* Q, const __half* K, const __half* V, __half* O,
    int batchSize, int seqLenQ, int seqLenKV, int numHeadsQ, int numHeadsKV,
    cudaStream_t stream);

template void runFwd<SM8x, __half, 128, true>(
    const __half* Q, const __half* K, const __half* V, __half* O,
    int batchSize, int seqLenQ, int seqLenKV, int numHeadsQ, int numHeadsKV,
    cudaStream_t stream);

template void runFwdVarLen<SM8x, __half, 128, false>(
    const __half* Q, const __half* K, const __half* V, __half* O,
    const int* cuSeqLensQ, const int* cuSeqLensKV,
    int batchSize, int maxSeqLenQ, int maxSeqLenKV,
    int numHeadsQ, int numHeadsKV,
    cudaStream_t stream);

template void runFwdVarLen<SM8x, __half, 128, true>(
    const __half* Q, const __half* K, const __half* V, __half* O,
    const int* cuSeqLensQ, const int* cuSeqLensKV,
    int batchSize, int maxSeqLenQ, int maxSeqLenKV,
    int numHeadsQ, int numHeadsKV,
    cudaStream_t stream);

template void runFwdPagedVarLen<SM8x, __half, 128, false>(
    const __half* Q, __half* O,
    const __half* kCachePool, const __half* vCachePool,
    const int* cuSeqLensQ, const int* cuSeqLensKV,
    const int* blockTable,
    int batchSize, int maxSeqLenQ, int maxSeqLenKV,
    int numHeadsQ, int numHeadsKV,
    int pageSize, int maxBlocksPerSeq,
    float* tmpO, float* tmpLse, int partitionSize, int totalQ,
    cudaStream_t stream);

template void runFwdPagedVarLen<SM8x, __half, 128, true>(
    const __half* Q, __half* O,
    const __half* kCachePool, const __half* vCachePool,
    const int* cuSeqLensQ, const int* cuSeqLensKV,
    const int* blockTable,
    int batchSize, int maxSeqLenQ, int maxSeqLenKV,
    int numHeadsQ, int numHeadsKV,
    int pageSize, int maxBlocksPerSeq,
    float* tmpO, float* tmpLse, int partitionSize, int totalQ,
    cudaStream_t stream);
#endif

#ifdef TFA_TARGET_HEADDIM_192
template void runFwd<SM8x, __half, 192, false>(
    const __half* Q, const __half* K, const __half* V, __half* O,
    int batchSize, int seqLenQ, int seqLenKV, int numHeadsQ, int numHeadsKV,
    cudaStream_t stream);

template void runFwd<SM8x, __half, 192, true>(
    const __half* Q, const __half* K, const __half* V, __half* O,
    int batchSize, int seqLenQ, int seqLenKV, int numHeadsQ, int numHeadsKV,
    cudaStream_t stream);

template void runFwdVarLen<SM8x, __half, 192, false>(
    const __half* Q, const __half* K, const __half* V, __half* O,
    const int* cuSeqLensQ, const int* cuSeqLensKV,
    int batchSize, int maxSeqLenQ, int maxSeqLenKV,
    int numHeadsQ, int numHeadsKV,
    cudaStream_t stream);

template void runFwdVarLen<SM8x, __half, 192, true>(
    const __half* Q, const __half* K, const __half* V, __half* O,
    const int* cuSeqLensQ, const int* cuSeqLensKV,
    int batchSize, int maxSeqLenQ, int maxSeqLenKV,
    int numHeadsQ, int numHeadsKV,
    cudaStream_t stream);

template void runFwdPagedVarLen<SM8x, __half, 192, false>(
    const __half* Q, __half* O,
    const __half* kCachePool, const __half* vCachePool,
    const int* cuSeqLensQ, const int* cuSeqLensKV,
    const int* blockTable,
    int batchSize, int maxSeqLenQ, int maxSeqLenKV,
    int numHeadsQ, int numHeadsKV,
    int pageSize, int maxBlocksPerSeq,
    float* tmpO, float* tmpLse, int partitionSize, int totalQ,
    cudaStream_t stream);

template void runFwdPagedVarLen<SM8x, __half, 192, true>(
    const __half* Q, __half* O,
    const __half* kCachePool, const __half* vCachePool,
    const int* cuSeqLensQ, const int* cuSeqLensKV,
    const int* blockTable,
    int batchSize, int maxSeqLenQ, int maxSeqLenKV,
    int numHeadsQ, int numHeadsKV,
    int pageSize, int maxBlocksPerSeq,
    float* tmpO, float* tmpLse, int partitionSize, int totalQ,
    cudaStream_t stream);
#endif

#ifdef TFA_TARGET_HEADDIM_256
template void runFwd<SM8x, __half, 256, false>(
    const __half* Q, const __half* K, const __half* V, __half* O,
    int batchSize, int seqLenQ, int seqLenKV, int numHeadsQ, int numHeadsKV,
    cudaStream_t stream);

template void runFwd<SM8x, __half, 256, true>(
    const __half* Q, const __half* K, const __half* V, __half* O,
    int batchSize, int seqLenQ, int seqLenKV, int numHeadsQ, int numHeadsKV,
    cudaStream_t stream);

template void runFwdVarLen<SM8x, __half, 256, false>(
    const __half* Q, const __half* K, const __half* V, __half* O,
    const int* cuSeqLensQ, const int* cuSeqLensKV,
    int batchSize, int maxSeqLenQ, int maxSeqLenKV,
    int numHeadsQ, int numHeadsKV,
    cudaStream_t stream);

template void runFwdVarLen<SM8x, __half, 256, true>(
    const __half* Q, const __half* K, const __half* V, __half* O,
    const int* cuSeqLensQ, const int* cuSeqLensKV,
    int batchSize, int maxSeqLenQ, int maxSeqLenKV,
    int numHeadsQ, int numHeadsKV,
    cudaStream_t stream);

template void runFwdPagedVarLen<SM8x, __half, 256, false>(
    const __half* Q, __half* O,
    const __half* kCachePool, const __half* vCachePool,
    const int* cuSeqLensQ, const int* cuSeqLensKV,
    const int* blockTable,
    int batchSize, int maxSeqLenQ, int maxSeqLenKV,
    int numHeadsQ, int numHeadsKV,
    int pageSize, int maxBlocksPerSeq,
    float* tmpO, float* tmpLse, int partitionSize, int totalQ,
    cudaStream_t stream);

template void runFwdPagedVarLen<SM8x, __half, 256, true>(
    const __half* Q, __half* O,
    const __half* kCachePool, const __half* vCachePool,
    const int* cuSeqLensQ, const int* cuSeqLensKV,
    const int* blockTable,
    int batchSize, int maxSeqLenQ, int maxSeqLenKV,
    int numHeadsQ, int numHeadsKV,
    int pageSize, int maxBlocksPerSeq,
    float* tmpO, float* tmpLse, int partitionSize, int totalQ,
    cudaStream_t stream);
#endif

// clang-format on

}  // namespace tfa::impl
